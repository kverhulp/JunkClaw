{
  "name": "@junkclaw/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "build:unpacked": "wxt build && rm -rf build && cp -R .output/chrome-mv3 build",
    "zip": "wxt zip",
    "postinstall": "wxt prepare",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource/archivo": "^5.3.0",
    "@junkclaw/core": "workspace:*",
    "@junkclaw/schema": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wxt": "^0.21.4"
  }
}
